using System;
using UnityEngine;

namespace Kai_Minigame
{
    public class BallLauncher : MonoBehaviour
    {
        [SerializeField]
        private BallGameManager ballGameManager;

        private void Update()
        {
            //Iterate instead of random.
        }
    }
}
